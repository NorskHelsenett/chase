{{- range $key, $value := .Values }}
  {{- $enabled := default true $value.enabled }}
  {{- if and $enabled $value.mounts }}
    {{- range $value.mounts }}
      {{- if and .config .config.data }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .name }}
data:
  {{ .config.fileName }}: |-
{{ .config.data | nindent 4 }}
---
      {{- end }}
    {{- end }}
  {{- end }}
{{- end }}
